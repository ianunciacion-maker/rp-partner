/**
 * Remotion entry point for rendering video compositions.
 * This file is used by the Remotion CLI.
 */
import { registerRoot } from "remotion";
import { RemotionRoot } from "./Root";

registerRoot(RemotionRoot);
